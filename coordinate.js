class coordinateSystem {
  constructor(startingX, startingY, startingZ){
    let vector = createVector(startingX, startingY, startingZ)
    
  }
  draw(){
    
  }
  setPos(newX, newY, newZ){
    vector.set(newX, newY, newZ)
  }
  getVector(){
    
  }
}